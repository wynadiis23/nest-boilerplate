name: Release

on:
  push:
    branches:
      - main

jobs:
  release:
    runs-on: semver
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Set up Node.js (Newer version)
        uses: actions/setup-node@v3
        with:
          node-version: 'latest' # Use the required Node.js version for semantic-release

      - name: Install semantic-release (latest version)
        run: |
          yarn add -D semantic-release @semantic-release/git @semantic-release/changelog @semantic-release/commit-analyzer @semantic-release/release-notes-generator @semantic-release/github

      - name: Run semantic-release
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: npx semantic-release
